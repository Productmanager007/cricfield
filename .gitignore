# Python
.venv/
__pycache__/
*.pyc

# Data — never commit the ball store or generated samples
data/
sample_data/
*.parquet
*.csv

# ...except the published IPL leaderboard (45 KB), which is a result, not raw data
!ipl_fraa.csv

# OS / editor
.DS_Store

# Secrets
.env
